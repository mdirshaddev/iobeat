name: '👨‍🚀 Ready for Development'

on:
  push:
    branches:
      - 'development'

jobs:
  development:
    name: '🔍 Check for Code Quality'
    uses: mdirshaddev/iobeat/.github/workflows/codeql.yml
